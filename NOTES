- HA not supported globally (but still handled at the cluster level)
- Figure out how to handle multiple verticles/instances and thread safety
- Fix codec name implementation
- Fix failures implementation
- Fix reply headers implementation
- Implement replyAndRequest
- Fix websocket server state management
- Handle timeout correctly when replied not received (reply exception for sender)
- Handler all timeouts correctly
- Fix invocation contexts (capture in Vert.x 3, use correct promise in Vert.x 4)
- Test all codecs
- Test all failure cases (timeout, explicit failure, runtime exception)
- Make EventBusLink thread-safe